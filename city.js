import * as THREE from 'three';export function createCity(scene){const g=new THREE.Mesh(new THREE.PlaneGeometry(120,120),new THREE.MeshStandardMaterial({color:0x4f8a4a}));g.rotation.x=-Math.PI/2;scene.add(g);const m=new THREE.MeshStandardMaterial({color:0x303030});for(const z of [0,20,-20]){const r=new THREE.Mesh(new THREE.BoxGeometry(120,.08,7),m);r.position.set(0,.04,z);scene.add(r)}for(const x of [0,20,-20]){const r=new THREE.Mesh(new THREE.BoxGeometry(7,.08,120),m);r.position.set(x,.05,0);scene.add(r)}}